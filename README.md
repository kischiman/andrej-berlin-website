# Andrej Berlin Website

Static personal website built from the provided consultancy HTML document.

Open `index.html` in a browser to view it. The generated hero asset is stored in `assets/hero-experience-lab.png`, and the source reference copy is currently in `assets/andrej-consultancy.html`.

Before publishing, replace the placeholder `mailto:hello@example.com` contact address in `index.html`.
